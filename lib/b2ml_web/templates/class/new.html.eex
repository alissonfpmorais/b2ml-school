<h1><%= gettext "Create new class" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.class_path(@conn, :create)) %>

<span><%= link gettext("Back"), to: Routes.class_path(@conn, :index) %></span>
