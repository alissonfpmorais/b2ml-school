<h1><%= gettext "Edit Class" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.class_path(@conn, :update, @class)) %>

<span><%= link gettext("Back"), to: Routes.class_path(@conn, :index) %></span>
