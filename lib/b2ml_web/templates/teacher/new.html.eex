<h1><%= gettext "Add new teacher" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.teacher_path(@conn, :create)) %>

<span><%= link gettext("Back"), to: Routes.teacher_path(@conn, :index) %></span>
