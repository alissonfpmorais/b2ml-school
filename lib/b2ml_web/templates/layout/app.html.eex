<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= assigns[:page_title] || "B2ml School!" %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul class="row">
            <li class="column"><%= link "Home", to: Routes.page_path(@conn, :index) %></li>
            <li class="column">
              <%= link to: Routes.page_path(@conn, :index, current_path: @conn.request_path, locale: "pt_BR"), class: "flag" do %>
                <img class="flag-img" src="<%= Routes.static_path(@conn, "/images/pt_BR.png") %>" alt="Portuguese">
              <% end %>
            </li>
            <li class="column">
              <%= link to: Routes.page_path(@conn, :index, current_path: @conn.request_path, locale: "en"), class: "flag" do %>
                <img class="flag-img" src="<%= Routes.static_path(@conn, "/images/en_US.png") %>" alt="English">
              <% end %>
            </li>
          </ul>
        </nav>
        <a href="https://b2ml.com.br" class="b2ml-logo">
          <img src="<%= Routes.static_path(@conn, "/images/b2ml.png") %>" alt="B2ml Logo"/>
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
